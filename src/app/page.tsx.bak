import React from 'react';
import Header from "@/components/Header";
import NovelSection from "@/components/NovelSection";
import Footer from "@/components/Footer";

const NewsCard = ({ source, title, summary, url }: any) => {
  return (
    <div className="group border-b border-gray-100 py-6 font-sans">
      <a href={url} target="_blank" rel="noopener noreferrer">
        <div className="flex items-center gap-2 mb-2">
          <span className="text-xs font-bold text-gray-400 uppercase tracking-tighter">{source}</span>
        </div>
        <h3 className="text-xl font-bold mb-2 group-hover:text-blue-600 transition-colors leading-snug tracking-tight">
          {title}
        </h3>
        <p className="text-gray-500 text-sm line-clamp-2 font-light">{summary}</p>
      </a>
    </div>
  );
};

export default function Home() {
  return (
    <div className="flex flex-col items-center min-h-screen bg-white">
      <Header />
      
      <main className="w-full max-w-screen-xl px-4 md:px-8">
        {/* 1번 구역: 뉴스 */}
        <section id="news" className="mb-20">
          <h2 className="text-2xl font-black mb-8 border-b-2 border-black pb-2 inline-block">LATEST NEWS</h2>
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-x-12 gap-y-4">
            <NewsCard source="Reuters" title="글로벌 반도체 시장 지각변동" summary="AI 칩 수요 폭증으로 인한 공급망 재편 현황." url="https://reuters.com" />
            <NewsCard source="BBC" title="에너지 혁명: 핵융합 기술" summary="탄소 제로 시대를 앞당길 새로운 에너지원." url="https://bbc.com" />
            <NewsCard source="Tech" title="차세대 AI 에이전트 출현" summary="스스로 행동하는 AI 시스템의 탄생." url="https://techcrunch.com" />
          </div>
        </section>

        <NovelSection />

        {/* 3번 구역: 트렌딩 */}
        <section id="trending" className="mb-20 w-full">
          <div className="bg-black text-white p-16 text-center">
            <h2 className="text-5xl font-black mb-6">PREMIUM AI TOOLKIT</h2>
            <p className="text-gray-400 mb-8 uppercase tracking-widest font-bold text-xs">Aether Press Selection</p>
            <button className="bg-white text-black px-12 py-4 font-black hover:bg-gray-200">BUY NOW</button>
          </div>
        </section>
      </main>

      <Footer />
    </div>
  );
}